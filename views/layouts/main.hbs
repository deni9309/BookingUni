<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <title>{{title}}</title>
        <link rel="stylesheet" href="/static/css/style.css">
    </head>

    <body>
        <div id="container">

            {{#if errors}}
                {{#each errors}}
                    <div>
                        <div class="notification loadingBox">
                            Loading &hellip;
                        </div>
                        <div class="notification infoBox">
                            <span>Info</span>
                        </div>
                        <div class="notification errorBox">
                            <span>{{this}}</span>
                        </div>
                    </div>
                {{/each}}
            {{/if}}
            <nav>
                <div class="left-container">
                    <ul>
                        <li><a href="/">Home</a></li>
                        {{#if isAuthenticated}}
                            <li><a href="/hotel/create">Add +</a></li>
                        {{else}}
                            <li><a href="/auth/login">Login</a></li>
                            <li><a href="/auth/register">Register</a></li>
                        {{/if}}
                    </ul>
                </div>
                <div class="right-container">
                    {{#if isAuthenticated}}
                        <a href="/profile" title="Go to profile page" class="log-out">Hi, {{user.username}}</a>
                        <a href="/auth/logout" class="log-out">Logout</a>
                    {{/if}}
                </div>
            </nav>

            {{{body}}}

            <footer>@SoftUni - JS Back-end - BookingUni 2020</footer>
        </div>
    </body>

</html>